# Current Routes (from `api/routes.py`)

One per line: `METHODS PATH -> handler`

- POST /admin/init-db -> admin_init_db
- POST /admin/recreate-db -> admin_recreate_db
- POST /admin/stop-populate -> admin_stop_populate
- GET / -> home_page
- GET /check-cik -> check_cik_page
- GET,POST /admin -> admin_page
- GET /daily-values -> daily_values_page
- GET /db-check -> db_check
- GET /sql -> db_check
